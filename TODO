Collect nodes

v

generate values from nodes
  - let functions decide whether they want to generate a value
    - include some helper functions to help
  - attach an array on target
  - generate an array of values on Tester for easy testing?

v

grade the values
  - traverse the bullseye
    - check for values
      - run grading callback against values
        - callback reports to a gradebook object!
          - targets run against, values, expected, passed



Properties:
* CSS hypothetical
* Position relative to:
  document, viewport, other elements

Compare network response:
* bytes by file
* bytes by filetype
* time

Misc:
* Page size

grunt / gulp:
* dev with lots of logging
* remove logs for prod (but still publish dev on gh-pages)