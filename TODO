Overall
-------

Properties:
* CSS hypothetical
* Position relative to:
  document, viewport, other elements

Compare network response:
* bytes by file
* bytes by filetype
* time
* Page size

New:
* better UI
	* separate window?
	* z-index the shit out of the widget
	* placeholder when tests results are hidden or missing
	* 'Test' vs 'Tests' if a suite only has one test
* prevent a test with an error to rerun
  * more useful error types

* show current on widget
	* get debug output
* more limits

Documenting:
* new README

Debugging:
* debug mode

Fixes:
* absolute position gives position relative to viewport. is that desired behavior?
* expand toEqual to match arrays and objects as well

Performance:
* cssProperty creates a FSL