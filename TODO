Collect nodes

v

generate values from nodes
  - let functions decide whether they want to generate a value based on node passed to it
    - include some helper functions to help run against specific kinds of nodes
  - attach a value to target
  - generate an array of values on Tester for easy testing?

v

grade the values
  - traverse the bullseye
    - check for values
      - run grading callback against values
        - callback reports to a gradebook object!
          - targets run against, values, expected, passed
  - gradebook checks results of callbacks and returns final result

Properties:
* CSS hypothetical
* Position relative to:
  document, viewport, other elements

Compare network response:
* bytes by file
* bytes by filetype
* time

Misc:
* Page size