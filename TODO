Weirdness:
* can I get rid of needToIterate?

Properties:
* CSS hypothetical
* Position relative to:
  document, viewport, other elements

Compare network response:
* bytes by file
* bytes by filetype
* time

Misc:
* Page size

grunt / gulp:
* dev with lots of logging
* remove logs for prod (but still publish dev on gh-pages)